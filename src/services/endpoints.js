const BASE_URL = "http://localhost:4000/";

const endpoints = {
  usuarios: `${BASE_URL}usuarios`,
  productos: `${BASE_URL}productos`,
  compras: `${BASE_URL}compras`,
  ofertas: `${BASE_URL}ofertas`,
};

export default endpoints;
